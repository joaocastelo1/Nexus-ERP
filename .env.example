# 🔷 MongoDB Atlas (opcional — se não configurado, usa banco em memória)
# Crie um cluster gratuito em https://mongodb.com/atlas
# Substitua <password> pela senha do seu usuário de banco de dados
# MONGODB_URI=mongodb+srv://<usuario>:<password>@cluster0.xxxxx.mongodb.net/nexus-erp?retryWrites=true&w=majority

# 🖥 Servidor local
PORT=5000
